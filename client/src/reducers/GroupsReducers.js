const defaultState = {
    title: '',
    desc: '',
    posts: [
        {
            content: '',
            datetime: ''
        }
    ]
};

const groupReducer = (state = defaultState, action) => {
    switch (action.type) {
        
    }
};